---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import { calculateReadingTime } from '../../utils/readingTime';

export async function getStaticPaths() {
	const posts = await getCollection('blog');
	return posts.map((post) => ({
		params: { slug: post.slug },
		props: post,
	}));
}

const post = Astro.props;
const { Content } = await post.render();
const readingTime = calculateReadingTime(post.body || "");
---

<Layout title={post.data.title} description={post.data.description}>
	<article class="max-w-4xl mx-auto">
        <header class="mb-16 border-b-4 border-muted pb-6">
            <h1 class="text-4xl md:text-5xl font-black leading-tight mb-4">{post.data.title}</h1>
            
            <div class="flex items-center gap-4 text-muted font-bold text-lg mb-4">
                <time datetime={post.data.pubDate.toISOString()}>
                    {post.data.pubDate.toLocaleDateString('en-us', {
                        year: 'numeric',
                        month: 'long',
                        day: 'numeric',
                    })}
                </time>
                <span>•</span>
                <span>
                    {readingTime}
                </span>
            </div>
            
            {post.data.heroImage && (
                <div class="relative mt-8">
                     <div class="absolute inset-0 bg-ink translate-x-3 translate-y-3 rounded-lg"></div>
                    <img width={1020} height={510} src={post.data.heroImage} alt="" class="relative border-4 border-brutal w-full rounded-lg" />
                </div>
            )}
        </header>
        
		<div class="prose prose-xl prose-neutral max-w-none prose-headings:font-black prose-p:font-medium prose-a:text-primary prose-a:font-bold prose-img:border-4 prose-img:shadow-[8px_8px_0px_0px_var(--color-shadow-color)] prose-img:rounded-lg mt-12">
			<Content />
		</div>
	</article>
</Layout>
